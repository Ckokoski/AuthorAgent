Refactor (Level 1, Stage B part 2): extract settings + projects mounters

Behavior-preserving; verified tsc + route-registration diff (234/234 identical).
routes.ts: ~5,518 -> 4,128 lines.

- routes/settings.routes.ts (mountSettings): memory reset, vault key CRUD +
  provider refresh, sanitized config read + whitelisted update, Telegram bridge.
- routes/projects.routes.ts (mountProjects): the Project Engine section (templates,
  project + pipeline creation, start/auto-execute incl. the local helpers
  buildStepUserMessage/getSmartExcerpt/stepNeedsFullManuscript, retry/restart/
  resume/pause, file listing/download, DOCX export, compile, provider override).
- routes.ts calls mountSettings + mountProjects.

Large sections are lifted verbatim via sed (handlers close over services/gateway/
baseDir, not `this`, so no renaming is needed) and wrapped — fidelity-guaranteed.
Each step verified by diffing every app.METHOD('path') vs the pre-split baseline.

Remaining mounters: documents (+ the cross-cutting `upload` multer), heartbeat,
personas, media (research/image/tts), ops (lessons/prefs/orchestrator), export
(+ cross-cutting `gatherChapters`, `upload`), wave2/3 (+ `gatherChapters`,
`addWaveDisclaimer`), knowledge, website. The 3 cross-cutting helpers get lifted
into routes/_shared.ts next. Final gate: Mercury rebuild + tests/openrouter-pipeline.sh.
