Complete project rename: finalize Stage D + close out tracking

- Mark RENAME-PLAN.md as COMPLETE: Stage D (local dir mv) done, all
  post-mv validation boxes checked (remote, grep, .env, tsc, smoke 18/18)
- Move the RENAME-PLAN item out of docs/TODO.md into docs/COMPLETED.md
  under 2026-05-31, preserving the original bullet text plus outcome
- Drop the now-empty "Pending plans" section from docs/TODO.md
- Rename the build-watcher systemd unit files authorclaw-build.{service,
  timer} -> bookclaw-build.{service,timer} (the rename sed updated their
  contents but not the filenames); now matches the README install commands
  and the bookclaw-build.timer -> bookclaw-build.service pairing
