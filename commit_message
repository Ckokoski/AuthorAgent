Add OpenRouter pipeline test, TODO items, and .keys ignore

- tests/openrouter-pipeline.sh: non-hermetic, manually-run verification that
  drives a bounded novel pipeline through OpenRouter only (Ollama disabled via
  an EXIT-trap that restores it) to confirm every pipeline task type — incl.
  reasoning-high (consistency, final_edit) and 16K-output steps — completes.
  Parameterized (BASE_URL / BOOKCLAW_AUTH_TOKEN / CONTAINER / CHAPTERS / WORDS)
- docs/TODO.md: add OpenRouter key auto-register bug (routes.ts:257), per-step
  model selection (Larger items), API-first frontend principle (enables MCP),
  and a new "User experience & product features" group (start-a-project
  tutorial, in-app help system, markdown file viewer)
- .gitignore: ignore .keys (local API-key stash for testing)
